name: cue
description: "Physics-first Flutter animations with a composable, timeline-driven API"
version: 0.1.0
homepage: https://github.com/Milad-Akarie/cue
topics: 
  - animation
  - spring
  - timeline
  - keyframes
  - effects
  - ui

environment:
  sdk: ^3.10.8
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  coverage: ^1.15.0 
  flutter_lints: ^6.0.0
 